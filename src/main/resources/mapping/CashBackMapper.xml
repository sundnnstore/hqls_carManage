<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="com.sinoauto.dao.mapper.CashBackMapper">

	<!-- 用于select查询公用抽取的列 -->
	<sql id="allColumns">
        cash_back_id, money, discount, return_money, return_type, is_usable, create_time
	</sql>

	<!-- 新增 -->
	<insert id="insert" parameterType="HqlsCashBack" useGeneratedKeys="true" keyProperty="id">
	    INSERT INTO hqls_cash_back (
	        money, discount, return_money, return_type, is_usable, create_time
	    ) VALUES (
	        #{money}, #{discount}, #{returnMoney}, #{returnType}, #{isUsable}, #{createTime}        
	    )
	</insert>

	<!-- 修改 -->
	<update id="update" parameterType="HqlsCashBack">
	   UPDATE hqls_cash_back SET
		   money = #{money}, 
		   discount = #{discount}, 
		   return_money = #{returnMoney}, 
		   return_type = #{returnType}, 
		   is_usable = #{isUsable}, 
		   create_time = #{createTime}
	   WHERE 
		   cash_back_id = #{cashBackId}
	</update>
	
</mapper>
