<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="com.sinoauto.dao.mapper.ServiceOrderMapper">

	<!-- 用于select查询公用抽取的列 -->
	<sql id="allColumns">
        service_order_id, order_no, customer_id, service_type_id, store_id, order_status, car_no, car_model, order_amount, create_time, dml_time
	</sql>

	<!-- 新增 -->
	<insert id="insert" parameterType="HqlsServiceOrder" useGeneratedKeys="true" keyProperty="id">
	    INSERT INTO hqls_service_order (
	        order_no, customer_id, service_type_id, store_id, order_status, car_no, car_model, order_amount, create_time, dml_time
	    ) VALUES (
	        #{orderNo}, #{customerId}, #{serviceTypeId}, #{storeId}, #{orderStatus}, #{carNo}, #{carModel}, #{orderAmount}, #{createTime}, #{dmlTime}        
	    )
	</insert>

	<!-- 修改 -->
	<update id="update" parameterType="HqlsServiceOrder">
	   UPDATE hqls_service_order SET
		   order_no = #{orderNo}, 
		   customer_id = #{customerId}, 
		   service_type_id = #{serviceTypeId}, 
		   store_id = #{storeId}, 
		   order_status = #{orderStatus}, 
		   car_no = #{carNo}, 
		   car_model = #{carModel}, 
		   order_amount = #{orderAmount}, 
		   create_time = #{createTime}, 
		   dml_time = #{dmlTime}
	   WHERE 
		   service_order_id = #{serviceOrderId}
	</update>
	
</mapper>
