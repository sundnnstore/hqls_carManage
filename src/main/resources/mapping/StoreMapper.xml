<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="com.sinoauto.dao.mapper.StoreMapper">

	<!-- 用于select查询公用抽取的列 -->
	<sql id="allColumns">
        store_id, pid, store_name, mobile, longitude, latitude, store_code, province_id, city_id, county_id, address, back_url, is_useable, create_time, dml_time
	</sql>

	<!-- 新增 -->
	<insert id="insert" parameterType="HqlsStore" useGeneratedKeys="true" keyProperty="id">
	    INSERT INTO hqls_store (
	        pid, store_name, mobile, longitude, latitude, store_code, province_id, city_id, county_id, address, back_url, is_useable, create_time, dml_time
	    ) VALUES (
	        #{pid}, #{storeName}, #{mobile}, #{longitude}, #{latitude}, #{storeCode}, #{provinceId}, #{cityId}, #{countyId}, #{address}, #{backUrl}, #{isUseable}, #{createTime}, #{dmlTime}        
	    )
	</insert>

	<!-- 修改 -->
	<update id="update" parameterType="HqlsStore">
	   UPDATE hqls_store SET
		   pid = #{pid}, 
		   store_name = #{storeName}, 
		   mobile = #{mobile}, 
		   longitude = #{longitude}, 
		   latitude = #{latitude}, 
		   store_code = #{storeCode}, 
		   province_id = #{provinceId}, 
		   city_id = #{cityId}, 
		   county_id = #{countyId}, 
		   address = #{address}, 
		   back_url = #{backUrl}, 
		   is_useable = #{isUseable}, 
		   create_time = #{createTime}, 
		   dml_time = #{dmlTime}
	   WHERE 
		   store_id = #{storeId}
	</update>
	
</mapper>
